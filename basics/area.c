#include <stdio.h>
int main()
{
  int l, b, area;
  printf("Enter length:");
  scanf("%d", &l);
  printf("Enter breadth:");
  scanf("%d", &b);
  area=l*b;
  printf("area of rectangle is %d", area);
  return 0;
}
